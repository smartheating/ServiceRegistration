spring:
  application:
    name: serviceregistration

server:
  port: ${PORT:9000}
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://localhost:9000/eureka